package lowercase

// ToLower returns the +in+ string lowercased. The only case conversion
// that this performs is converting A-Z to a-z (unicode or extended ASCII
// characters are not converted).
func ToLower(in string) string {
	return "implement me"
}
